<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.BeatBox" parent="Theme.AppCompat">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/red</item>
        <item name="colorPrimaryVariant">@color/dark_red</item>
        <item name="colorAccent">@color/gray</item>
        <item name="android:windowBackground">@color/soothing_blue</item>
    </style>
    <style name="BeatBoxButton">
        <item name="android:background">@drawable/button_beat_box</item>
    </style>
    <!--<style name="StrongBeatBoxButton" parent="@style/BeatBoxButton">  <- Альтернативный вариант с указанием родителя | -->
    <style name="BeatBoxButton.Strong">
        <item name="android:textStyle">bold</item>
    </style>
</resources>